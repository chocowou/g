#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<int> num_list, int n) {
    vector<int> answer;
    int s=num_list.size();
    for(int i=0;i<s;i++){
        answer.push_back(num_list[(n+i)%s]);
    }
    return answer;
}